var mongoose = require('mongoose');

var Schema = mongoose.Schema;

var geoSchema = mongoose.Schema({
	//two points: lattitude and longditude
	lattitude : Number,
	longditude : Number,
	imageID  : String
});

//var  = mongoose.model('Image', imageSchema);


// //mongoose.connect('mongodb://localhost:27017/boomerDB'); 
module.exports = mongoose.model('locations', geoSchema);  