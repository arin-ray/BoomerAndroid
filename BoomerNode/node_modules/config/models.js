/**
 * Boomer
 *
 * @module      :: Model
 * @description :: Represents schemas for users, pics, etc.
 * @author      :: Arin BigPlay Ray
 */
var mongoose = require('mongoose');

var Schema = mongoose.Schema;

var userSchema = mongoose.Schema({ 
	name : String,
	nick: String, 
	reg_id: String,
	friends: {
      type: Array,
      'default': []
    }
});

module.exports = mongoose.model('users', userSchema);      

